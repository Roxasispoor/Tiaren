public enum CompetenceType
{
    /// <summary>
    /// compétence utilisée sur soi
    /// </summary>
    SELFCOMPETENCE,
    /// <summary>
    /// Compétence qui nécessite un seul click sur un livingPlaceable pour être utilisée
    /// </summary>
    ONECLICKLIVING,
    /// <summary>
    /// Compétence qui nécessite deux clicks  sur des livingPlaceable pour être utilisée
    /// </summary>
    TWOCLICKLIVING,
    /// <summary>
    /// Compétence qui nécessite un seul click sur un Placeable pour être utilisée
    /// </summary>

    ONECLICKPLACEABLE,
    /// <summary>
    /// Compétence qui nécessite deux clicks  sur des Placeable pour être utilisée
    /// </summary>
    TWOCLICKPLACEABLE
}
